#include "main.h"
#include <string.h>
/**
 * _ strcat - concatenates two strings
 *
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
	char *dest;
	char *src;
	char *strcat;

	_strcat(dest, src);
	_putchar("_strcat(dest, src): %s\n", dest);
}
