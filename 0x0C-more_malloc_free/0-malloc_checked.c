#include "main.h"
#include <stdlib.h>
/**
 * malloc_checked - allocates memory using malloc
 * @b: int
 *
 * Return: 0
 */
void *malloc_checked(unsigned int b)
{
	unsigned int m;
	int *i;

	i = malloc(sizeof(int*) * b)
	for (m = 0; int[m] != '\0'; m++)
		;
	if (!malloc)
		exit(98);
	return(i);
}
