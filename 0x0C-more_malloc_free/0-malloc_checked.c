#include "main.h"
#include <stdlib.h>
/**
 * malloc_checked - allocates memory using malloc
 * @b: int
 *
 * Return: 0
 */
void *malloc_checked(unsigned int b)
{
	unsigned int m;
	int *i;

	m =malloc(sizeof(i) * b)
	if (malloc = NULL)
		return (98);
	return(0);
}
