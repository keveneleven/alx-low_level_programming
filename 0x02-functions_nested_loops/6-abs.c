#include "main.h"
/**
 *_abs - function that computes the absolute value of a computer
 * @a: integer input
 *
 * Return: absolute value of ab
 */
int _abs(int ab)
{
	return (ab * ((ab > 0) - (ab < 0)));
}
